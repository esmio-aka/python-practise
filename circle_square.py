# Prompt the user for the radius of the circle and store it as a floating-point number
radius = float(input('Enter the radius of a circle '))

# Calculate the area of the circle using the formula
area = 3.14159 * radius ** 2

# Print the result
print(f'Circle area is {area}')

